#include "main.h"
#include <stdio.h>

/**
 * add - function that adds two integers
 *
 * @a:is added to b
 * @b:is added to a
 *
 * Return:returns c at the end of program
 */

int add(int a, int b)
{
	int c;

	c = a + b;
	return (c);
}
